/*
 * 프론트 설정 — 여기 값만 채우면 반영됩니다. (코드 수정 불필요)
 *  - refreshMinutes  : 브라우저가 data.json 을 다시 불러오는 주기(분)
 *  - gaMeasurementId : Google Analytics 4 측정 ID (비우면 추적 비활성)
 *  - telegramUrl     : 헤더 텔레그램 버튼 링크 (비우면 버튼 숨김)
 */
window.SITE_CONFIG = {
  refreshMinutes: 30,
  gaMeasurementId: "",
  telegramUrl: "",
  // 최상단 고정 계정(계정/채널 username, 소문자). 여기 있는 계정 칸이 맨 앞에 옵니다.
  pinned: ["semianalysis"],
};
